JobLens - Jace Kendel

V1.0 - Setup backend, API, and frontend
     - Setup PostgreSQL DB for holding job posting property data
     - Identified job posting sites to scrape from (BuiltIn, Wellfound for now)
     - Created Scraping and parsing files for each site, utilized for data ingestion
     - Utilized pipeline, normalizer, dedeuplicator and validator to ensure cleanliness and checks
     - Successfully connected backend and frontend and displayed data on frontend
     - Modernized frontend (Better layout, filtering, company logos, source)

1.1  - Fixed job URL links to allow apply button to work
     - Fixed location for Wellfound jobs being a salary value instead of loc
     - Got some of Remote type working - switched it to bool value, but logic is wrong
          (all fields are false rn), if T/F then filter works for remote and can just display
          remote attribute on job card on JobLens

V1.1 - Expanded multi-source ingestion and improved scraping
     - Updated Wellfound parser to extract multiple job positions from a single company card
     - Added support for relative posting times including days, weeks, months, and years
     - Fixed source job ID handling for reliable database deduplication
     - Added Built In pagination to scrape multiple job listing pages instead of only the first 23 postings
     - Cleaned and organized Built In and Wellfound parser code for maintainability
     - Debugged and verified the full ingestion flow from scraping through PostgreSQL storage


TODO: - Gather 1-2 more sites 
      - Better filtering (Location filter doesnt work, add sort by newest, experience)
      - Will I add AI to this so it can find me best matched jobs? (No linkedin premium fees)